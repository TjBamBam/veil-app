/// <reference types="next" />
/// <reference types="next/image-types" />

// List correct vtype lib versions as documented
declare global {
  namespace Next {
    interface Config {
      // Next config
    }
  }
}

export {};
