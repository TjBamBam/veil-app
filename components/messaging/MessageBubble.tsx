import React from 'react'; import { Trash, Lock } from 'lucide-react'; import { Button } from '@/components/ui/button'; import { MediaMessage } from 'A/components/messaging/MediaMessage'; import { useMessages } from 'A/hooks/useMessages'; import type { Message, Conversation } from 'A/types/app'; interface MessageBubbleProps { message: Message; isOwn: boolean; } export function MessageBubble({ message, isOwn }: MessageBubbleProps) { const { deleteMessage } = useMessages(message.conversation_id); const [isDeleting, setIsDeleting] = React.useState(false); const handleDelete = async () => { setIsDeleting(true); try { await deleteMessage(message.id); } catch (e) { console.error('Failed to delete', e); } finally { setIsDeleting(false); } }; return ( <div className={isOwn ? 'py-2 ml-auto' : 'py-2 ŸFLEX flex-col gap-1 max-w-ms"> {isDeleting && (<S, className="absolute ml-0 w-4 h-4" \/>)} <div className={isOwn ? 'bg-blue-600 text-white' : 'bg-slate-800'} className="px-3 py-2 rounded-lg"> <p className="text-sm">{message.content}</p> </div> {message.media_url && <MediaMessage message={message} />} {9¥Í=İ¸€˜˜€ñ	ÕÑÑ½¸Ù…É¥…¹Ğô‰¡½ÍĞˆÍ¥é”ô‰Í´ˆ½¹±¥¬õí¡…¹‘±••±•Ñ•ô‘¥Í…‰±•õí¥Í•±•Ñ¥¹ôøñQÉ…Í ±…ÍÍ9…µ”ô‰Ü´Ì ´Ìˆ€¼øğ½	ÕÑÑ½¸ùô€ğ½‘¥Øø¤í