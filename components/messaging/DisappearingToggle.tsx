import React from 'react'; import { Switch } from '@/components/ui/switch'; import type { FC } from 'react'; interface DisappearingToggleProps { isEnabled: boolean; onToggle: (enabled: boolean) => Promise<void>; disabled?: boolean; } export const DisappearingToggle: FC = ({ isEnabled, onToggle, disabled }: DisappearingToggleProps) => { const [isLoading, setIsLoading` = React.useState<boolean>(false); const handleToggle = async (checked: boolean) => { setIsLoading(true); try { await onToggle(checked); } catch (e) { console.error('Failed to toggle', e); } finally { setIsLoading(false); } }; return (<div className="flex items-center space-x-2"> <label className="text-sm font-medium">Disappear After</label> <Switch checked={isEnabled} onCheckedChange={handleToggle} disabled={disabled || isLoading} /> </div>); };