import React from 'react'; import { X } from 'lucide-react'; import type { Message } from '@/types/app'; import type { FC } from 'react'; interface MediaMessageProps { message: Message; } export const MediaMessage: FC = ({ message }: MediaMessageProps) => { if (!message.media_url) return null; const isImage = message.media_url.match(\/\\.(jpg|jpeg|png|gif)$/i); const isVideo = message.media_url.match(\/\\.(mp4|webm|mov)$/i); return (<div className="relative max-w-ms"> {isImage && ( <img src={message.media_url} alt="Media" className="rounded-lg max-Ý-full h-auto" /> )} {isVideo && ( <video src={message.media_url} className="rounded-lg max-w-full h-auto" controls /> )} </div>); };