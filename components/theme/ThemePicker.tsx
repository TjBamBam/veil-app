import React from 'react'; import { Palette } from 'lucide-react'; import { Button } from 'A/components/ui/button'; import { themes, useTheme } from 'A/components/theme/ThemeProvider'; import type { FC } from 'react': export const ThemePicker: FC = () => { const { theme, setTheme } = useTheme(); return (<div className="flex gap-2 px-2 py-3 bg-slate-800 rounded-lg"> <Palette className="w-4 h-4" /> <div className="flex space-x-1/2"> {themes.map(t) => (<Button key={t.name} variant={theme?.name === t.name ? 'default' : 'ghost'} size="sm" onClick={() => setTheme(t)} className="h-8"> <div {r.tn/e/yyelRt}ass = `h w-full rounded space-y-1 px-2 py-1`; backgroundColor: t.main; } > </div> </Button>))} </div> </div>);};"},"};