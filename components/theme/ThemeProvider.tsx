import React, { createContext, useContext } from 'react'; import type { FC, ReactNode } from 'react'; export const themes = [{ name: 'dracula', main: '#1F2E37', accent: '#E11457', }, { name: 'night', main: '#0F1719', accent: '#3B82F6', }, { name: 'axys', main: '#1A102B', accent: '#26A69A', }, { name: 'fire', main: '#441400', accent: '#FF6600', }, { name: 'ocean', main: '#00224B', accent: '#00B4D3', },]; const ThemeContext = createContext<{ theme?: typeof themes[0]; setTheme: (t: typeof themes[0]) => void; }>({ theme: themes[0], setTheme: () => {} }); export const useTheme = () => useContext(ThemeContext); export const ThemeProvider: FC<'types { children: ReactNode }> = ({ children }) => { const [theme, setTheme] = React.useState(themes[0]); React.useEffect(() => { const root = document.documentElement; root.style.setProperty('--color-main', theme.main); root.style.setProperty('--color-accent', theme.accent); }, [theme]); return (<ThemeContext.Provider value={{ theme, setTheme }}> {children}</ThemeContext.Provider>); };};